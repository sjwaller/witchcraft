# macOS
.DS_Store

# Archives / local backups
openspec.zip
*.zip

# Rust build output (for when implementation lands)
/target
**/target

# Local model artifacts (GGUF weights are named only in manifests, never committed)
/models
*.gguf
